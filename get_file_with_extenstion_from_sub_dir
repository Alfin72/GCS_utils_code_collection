import os
def get_files(dir, extention):


    path =dir

    for root, dirs, files in os.walk(path):
        for file in files:
            if(file.endswith(extention)):
                print(os.path.join(root,file))